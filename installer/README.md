# Windows installer

Compile `DCSRadioVoiceControl.iss` with Inno Setup 7. The output is written to `dist\DCS-Radio-Voice-Control-Setup.exe`.

The installer runs per-user. Its only elevated operation is the existing verified DCS radio-menu integration.
